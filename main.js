const app = Vue.createApp({
    data(){
        return {
            product: {
                tittle: "Socks",
                description: "a normal gray sock with vue logo"

            }
        }
    } 
})
